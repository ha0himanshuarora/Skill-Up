import { BookOpen, Layers, BrainCircuit, Target, ListTodo, Palette, PenTool, Milestone, Flag, ClipboardCheck, TrendingUp, Rocket, type LucideIcon } from "lucide-react";
import type { ElementType } from 'react';

export const iconMap: { [key: string]: ElementType<LucideIcon> } = {
  BookOpen,
  Target,
  ListTodo,
  BrainCircuit,
  Layers,
  Palette,
  PenTool,
  Milestone,
  Flag,
  ClipboardCheck,
  TrendingUp,
  Rocket
};
